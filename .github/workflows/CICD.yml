name: GameStore Deploy

on:
  push:
    branches:
      - dev
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@master
       
    - name: Prepare WordPress Zip
      run: |
        WP_LINK=$(cat wp-version-control.cfg)
        wget -O "./wordpress.zip" $WP_LINK

    - name: Prepare plugins and theme zip
      run: zip -r wpcontent.zip plugins mu-plugins themes

    - name: Copy Zips to Server
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        password: ${{ secrets.SSH_PASSWORD }}
        port: ${{ secrets.SSH_PORT }}
        source: "./wordpress.zip, ./wpcontent.zip"
        target: ${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}

    - name: Enable Maintenance Mode
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        password: ${{ secrets.SSH_PASSWORD }}
        port: ${{ secrets.SSH_PORT }}
        source: ".maintenance"
        target: ${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}

    - name: Update Wordpress Core
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        password: ${{ secrets.SSH_PASSWORD }}
        port: ${{ secrets.SSH_PORT }}
        script: |
          find "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}" -type f -name "*.php" ! -name "wp-config.php" -delete
          rm -r "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wp-admin/" "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wp-includes/"
          unzip -o "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wordpress.zip" -d "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/"
          mv "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wordpress/"* "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/"
          rm -r "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wordpress"

    - name: Update Plugins and Theme
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        password: ${{ secrets.SSH_PASSWORD }}
        port: ${{ secrets.SSH_PORT }}
        script: |
          rm -rf "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wp-content/plugins" "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wp-content/mu-plugins" "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wp-content/themes"
          unzip "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wpcontent.zip" -d "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wp-content/"

    - name: Disable Maintenance Mode and Remove zips
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        password: ${{ secrets.SSH_PASSWORD }}
        port: ${{ secrets.SSH_PORT }}
        script: |
          rm -rf "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/.maintenance"
          rm -rf "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wpcontent.zip"
          rm -rf "${{ github.event_name == 'push' && 'dev.development-vse-avto.ru/public_html' || 'live.development-vse-avto.ru/public_html' }}/wordpress.zip"